name: Create a release

on:
  push:
    branches:
      - master
      - main

jobs:
  release:
    name: Create a release tag
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # - name: Setup pnpm
      #   uses: pnpm/action-setup@v2

      # - name: Setup node
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: 20

      - name: Check commit message for "release"
        id: check_commit_message
        run: echo "contains_release=$(if git log --format=%B -n 1 ${{ github.sha }} | grep -q 'release'; then echo true; else echo false; fi)" >> $GITHUB_OUTPUT

      - name: Get package name from commit message
        id: package_name
        if: steps.check_commit_message.outputs.contains_release == 'true'
        run: echo "package_name=$(git log --format=%B -n 1 | grep -o '^release(\(.*)\)' | sed 's/^release(\(.*\))/\1/')" >> $GITHUB_OUTPUT

      - name: Add release tag
        if: steps.check_commit_message.outputs.contains_release == 'true'
        id: tag_name
        run: sh ./scripts/release.sh
        env:
          PACKAGE_NAME: ${{ steps.package_name.outputs.package_name }}
          TOKEN: ${{ secrets.ACTION_SECRET }}